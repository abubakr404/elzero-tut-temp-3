section.Events#Events
  +specialHead("Latest " + otherLinksRight[0].name)
  img(src=imgsPath + Events.dots, alt="")
  img(src=imgsPath + Events.dots, alt="")
  .container
    img(src=imgsPath + Events.imgName, alt="")
    .event
      +downCount()
      .content
        h3=Events.title
        p=Events.text
    form(method="get")
      input(type="text" placeholder=Events.subscribe.placeholder)
      input(type="submit", value=Events.subscribe.button)
